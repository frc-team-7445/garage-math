#pragma once

#include <array>

namespace garage {
    namespace math {
        template<class T=double, int w, int h>
        struct matrix {
        private:
            std::array<std::array<T, w>, h> data;
        public:
dawd
        };
    }
}